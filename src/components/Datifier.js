import React, { useState } from 'react'

const Datifier = ({ type, day, month, year }) => {
    return (
        <div>
            This component will take date organising type, day, month and year as a prop and will turn that into a string of the desired type
        </div>
    )
}

export default Datifier